/* Defines a set of custom mallocs for Arena allocation using mmap()
* This allows for easy allocation and freeing of huge chunks of nested memory with similar lifetimes - e.g VM with its many attributes and structs
*/

#ifndef PRESEQL_MALLOC_H
#define PRESEQL_MALLOC_H

#endif
